extends layouts/main.pug
include mixins/videoBlock 
block content
    .vedios
        each item in video
            +videoBlock({
                id:item.id,
                title:item.title,
                views:item.views,
                videoFile:item.videoFile
            })